name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  lint-and-test:
    name: Lint and test
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v6
      - name: Lint
        run: make lint
      - name: Test
        run: make test
